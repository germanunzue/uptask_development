doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario    
            h1= nombrePagina 
            p Coloca tu nueva contraseña

            form.caja-login(method="POST" )
                .campo  
                    label(for="password") Contraseña: 
                    input(type="password" name="password" placeholder="Tu nueva contraseña" required )

                .campo.enviar
                    input.boton(type="submit" value="Reestablecer Nueva Contraseña")